# speech-based-data-entry
This Python script uses speech recognition and text-to-speech to collect a user's name, email, and message via voice input. It then sends the collected data to a Google Sheet using a webhook (Google Apps Script Web App URL). It provides voice feedback throughout the process using the `pyttsx3` library.
